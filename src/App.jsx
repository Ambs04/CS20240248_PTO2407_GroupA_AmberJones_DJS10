import { useState, useEffect } from "react";

function App() {
  //set data state
  const [state, setState] = useState([]);
  //set error state
  const [error, setError] = useState(null);

  useEffect(() => {});
}

export default App;
